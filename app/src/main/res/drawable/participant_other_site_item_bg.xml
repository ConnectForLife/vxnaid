<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Drawable for the selected state -->
    <item android:drawable="@color/participant_bg_selected" android:state_selected="true" />
    <!-- Drawable for the default (unselected) state -->
    <item android:drawable="@color/cardview_shadow_start_color" android:state_selected="false" />
</selector>